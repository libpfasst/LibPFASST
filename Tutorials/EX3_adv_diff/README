# Example program  calling libpfasst to solve the 1D advection-diffusion equation

    u_t + v*u_x = nu*u_xx

# Building the Tutorial:
Call `$ make` in the ../LIBPFASST/Tutorials/EX3_adv_diff.

# Running the Tutorial
Call `$ ./main.exe sdc.nml` for single level non-parallel SDC
Call `$ mpirun -n 4 ./main.exe sdc.nml` for parallel single level SDC


